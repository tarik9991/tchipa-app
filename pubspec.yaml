name: tchipa_app
description: "Tchipa - Verificateur de produits AliExpress/Temu avec IA"
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  qr_flutter: ^4.1.0
  share_plus: ^7.1.0
  path_provider: ^2.1.1
  screenshot: ^3.0.0
  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.1
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_animate: ^4.3.0
  lottie: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
